# Team crests

`team-crests.json` maps the normalized team names used by the simulator to local image files. The dashboard embeds these files as data URIs and therefore does not contact the internet when opened. If an image is absent, the dashboard renders a deterministic initials badge.
